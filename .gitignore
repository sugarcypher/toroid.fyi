# Originals — preserved on disk for reference, not deployed.
# The deploy serves the consolidated copies in poly/, sabbot/, play/, papers/, matrix/.
# Anchored to repo root so they don't also match nested deployed pages
# (e.g. predicates/sabbotarchy/, systems/opmanifold/).
/Polymorphic Matrix/
/Strat-Dom-3d/
/sabbotarchy/
/opmanifold/
opmanifold.zip
strat-dom-3d.rtf

# Root-level stragglers — now homed in their proper subdirs
# (matrix/circulatory_pressure_architectures.{docx,summary.pdf},
#  poly/multimalocracy_v{2,3}.pdf, poly/multimalocracy_revision_proposal.md).
# Keep these blocks here so the deploy stays clean even if root copies linger.
/Multimalocracy2.pdf
/multimalocracy3.pdf
/multimalocracy_revision_proposal.md
/circulatory_pressure_architectures.docx
/circulatory_pressure_architectures_summary.pdf

# Duplicate of matrix/index.html (verified identical via md5)
matrix/indexM.html

# HUD/build scratch — kept on disk for reference, not tracked or deployed.
# _hud_flush_bezel.html is a superseded bezel prototype; its only novel bit is
# roundedRectPoint() (rounded-rect perimeter distributor — abandoned in the v4
# corners-only HUD). BUILD_COMPARISON.html is a dated A-vs-B decision record
# already captured in project notes. _frame_preview.png is a render artifact.
# (play/hud-concept.html is the LIVE port-target and is intentionally NOT here.)
play/_hud_flush_bezel.html
play/_frame_preview.png
play/BUILD_COMPARISON.html

# OS noise
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride
Icon?

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local node_modules / build caches if any sneak in
node_modules/
dist/
build/
.cache/
.parcel-cache/
.next/
.nuxt/

# Cloudflare local dev + secrets
.wrangler/
.cf-deploy-secret
.env
.env.*

# Logs / temp
*.log
*.tmp

.dev.vars*
!.dev.vars.example
!.env.example
